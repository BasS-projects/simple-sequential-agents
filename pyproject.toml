[project]
name = "travel-policy-agents"
version = "0.1.0"
description = "LangGraph RAG assistant for international travel policy"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "langchain-openai>=1.0.0",
  "langgraph>=1.0.0",
  "python-dotenv>=1.0.0",
  "uvicorn>=0.30.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "ruff>=0.8.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
